# Copie este arquivo para .env e ajuste os valores.
# O .env NUNCA deve ser commitado — adicione ao .gitignore.
#
#   cp .env.example .env

# Senha do PostgreSQL (desenvolvimento local)
DB_PASSWORD=sentinela_dev_pass
